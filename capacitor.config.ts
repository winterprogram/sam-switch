import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'sam.switch.app',
  appName: 'iSamtree',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
